- present(@address) do |address|
  %h1
    = address.full_name
    = render 'actions_dropdown'
  %p= address.detail_line
  = address.notes_paragraph

  = render 'contacts'
  = render 'accounts'
  = render 'credit_agreements'
  - if @address.creditor? 
    = render 'pdfs'

