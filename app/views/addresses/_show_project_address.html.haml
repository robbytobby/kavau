%h2
  = t('addresses.show.contacts')
  -if policy(:contact).new?
    = icon_link_to :plus, new_project_address_contact_path(address), class: 'pull-right', id: 'add_contact'
= render :partial => 'addresses/contacts_table'

%h2
  = t('addresses.show.accounts')
  -if policy(:contact).new?
    = icon_link_to :plus, new_project_address_account_path(address), class: 'pull-right', id: 'add_account'
= render :partial => 'addresses/accounts_table'
