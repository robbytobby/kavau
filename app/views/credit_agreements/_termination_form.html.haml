= simple_form_for [@credit_agreement.creditor, @credit_agreement], wrapper: :inline_form, html: { class: 'form-inline' } do |f|
  zum
  = f.input :terminated_at, as: :date, label_html: { class: 'sr-only' }
  = f.submit t('.terminate'), class: 'btn btn-default btn-primary', id: :terminate
